{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "use-immer": "^0.9.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "esbuild src/index.tsx --bundle --outfile=dist/Bundle.js --loader:.ts=tsx --format=cjs",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/react-dom": "^18.2.18",
    "esbuild": "0.19.11",
    "eslint": "^8.56.0",
    "prettier": "3.1.1",
    "typescript": "^5.3.3"
  }
}
